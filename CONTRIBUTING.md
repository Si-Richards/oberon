# Contributing

Keep v0.9.1 research/paper-only. Changes that introduce authenticated exchange trading should be proposed separately and must include explicit safety controls, reconciliation, commission accounting and tests.

Run `make check` before submitting changes.
